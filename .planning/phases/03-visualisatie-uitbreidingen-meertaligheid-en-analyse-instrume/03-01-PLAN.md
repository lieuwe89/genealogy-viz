---
phase: 03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/js/i18n.js
  - public/js/i18n/nl.json
  - public/js/i18n/en.json
  - public/app.html
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "i18n module loads Dutch translations by default on page load"
    - "t(key) returns Dutch string when lang is nl"
    - "t(key) returns English string when lang is en"
    - "t(unknownKey) returns the key itself as fallback"
    - "Language preference persists in localStorage across page reload"
    - "Language toggle button visible in toolbar"
  artifacts:
    - path: "public/js/i18n.js"
      provides: "window.i18n module with t(), init(), setLang(), toggleLang(), applyToDOM(), getLang()"
      exports: ["window.i18n"]
    - path: "public/js/i18n/nl.json"
      provides: "Dutch translation strings"
      contains: "lang_toggle_label"
    - path: "public/js/i18n/en.json"
      provides: "English translation strings"
      contains: "lang_toggle_label"
  key_links:
    - from: "public/app.html"
      to: "public/js/i18n.js"
      via: "script tag before viz.js"
      pattern: "src=\"js/i18n.js\""
    - from: "public/js/i18n.js"
      to: "public/js/i18n/nl.json"
      via: "fetch() in init()"
      pattern: "fetch.*js/i18n/"
---

<objective>
Create the i18n framework module and language toggle button. This is the foundation that all other Phase 3 features depend on for translated labels.

Purpose: Enable Dutch/English UI switching per D-05 through D-07 and D-09. Other plans (clustering labels, relationship labels) will call `i18n.t(key)` from this module.
Output: `window.i18n` module, two JSON translation files, language toggle in toolbar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-CONTEXT.md
@.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-RESEARCH.md
@.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-UI-SPEC.md

<interfaces>
<!-- Established module pattern from existing code -->
From public/js/colour-modes.js:
```javascript
window.ColorModes = { buildSurnamePalette, getNodeColor };
```

From public/js/overlap-logic.js:
```javascript
window.OverlapLogic = { estimateLifespan, overlapLevel, shortestPath };
```

From public/app.html (toolbar structure, lines 13-18):
```html
<div id="toolbar">
  <button class="toolbar-btn active" id="btn-role" onclick="setColorMode('role')">By Role</button>
  <button class="toolbar-btn" id="btn-surname" onclick="setColorMode('surname')">By Surname</button>
  <button class="toolbar-btn" id="btn-sex" onclick="setColorMode('sex')">By Sex</button>
  <button class="toolbar-btn" id="btn-overlap" onclick="setColorMode('overlap')" title="Klik een persoon om overlappende tijdgenoten te zien">Overlap</button>
</div>
```

Script load order (bottom of body):
```html
<script src="js/colour-modes.js"></script>
<script src="js/overlap-logic.js"></script>
<script src="js/side-panel.js"></script>
<script src="js/viz.js"></script>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create i18n module and translation JSON files</name>
  <files>public/js/i18n.js, public/js/i18n/nl.json, public/js/i18n/en.json</files>
  <read_first>
    - public/js/colour-modes.js (reference for window.X module pattern)
    - public/js/viz.js (understand initViz flow and DOMContentLoaded pattern)
    - public/app.html (understand script load order and base href)
    - .planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-UI-SPEC.md (copywriting contract table)
    - .planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-RESEARCH.md (i18n module skeleton, Pattern 6, Pitfall 4-5)
  </read_first>
  <action>
Create three files:

**public/js/i18n.js** — `window.i18n` IIFE module (per D-05, D-07, D-09):
```javascript
'use strict';
window.i18n = (function () {
  let lang = 'nl';
  const cache = {};

  async function loadLang(l) {
    if (!cache[l]) {
      const res = await fetch('js/i18n/' + l + '.json');
      cache[l] = await res.json();
    }
  }

  async function init() {
    const stored = localStorage.getItem('lang');
    lang = stored || 'nl';
    await loadLang('nl');
    if (lang !== 'nl') await loadLang(lang);
    applyToDOM();
  }

  function t(key) {
    return (cache[lang] && cache[lang][key]) || (cache['nl'] && cache['nl'][key]) || key;
  }

  async function setLang(l) {
    await loadLang(l);
    lang = l;
    localStorage.setItem('lang', l);
    applyToDOM();
  }

  function toggleLang() {
    setLang(lang === 'nl' ? 'en' : 'nl');
  }

  function applyToDOM() {
    document.querySelectorAll('[data-i18n]').forEach(function(el) {
      el.textContent = t(el.dataset.i18n);
    });
    document.querySelectorAll('[data-i18n-title]').forEach(function(el) {
      el.title = t(el.dataset.i18nTitle);
    });
  }

  function getLang() { return lang; }

  return { init: init, t: t, setLang: setLang, toggleLang: toggleLang, applyToDOM: applyToDOM, getLang: getLang };
})();
```

**public/js/i18n/nl.json** — Dutch translations (canonical per D-09). Include ALL keys from the UI-SPEC copywriting contract table PLUS keys for existing hardcoded UI labels:
```json
{
  "lang_toggle_label": "EN",
  "cluster_auto": "Clusters: auto",
  "cluster_locked": "Clusters: vast",
  "cluster_level_label": "Clusterniveau",
  "cluster_centuries": "Eeuwen",
  "cluster_surnames": "Geslachtsnamen",
  "cluster_persons": "Personen",
  "cluster_expand_tooltip": "Klik om te openen",
  "rel_prefix": "Relatie:",
  "rel_ancestor": "Voorouder ({n} generaties)",
  "rel_descendant": "Nakomeling ({n} generaties)",
  "rel_spouse": "Echtgeno(o)t(e)",
  "rel_inlaw": "Aangetrouwd verwant (via {n} stappen)",
  "rel_complex": "Verwant via {n} stappen",
  "rel_no_path": "Geen verbinding gevonden",
  "rel_error": "Kon relatie niet bepalen",
  "comparison_heading": "Vergelijking",
  "rel_section_heading": "Relatie",
  "toolbar_role": "Op rol",
  "toolbar_surname": "Op achternaam",
  "toolbar_sex": "Op geslacht",
  "toolbar_overlap": "Overlap",
  "panel_vitals": "Vitals",
  "panel_roles": "Rollen",
  "panel_connections": "Verbindingen",
  "panel_notes": "Notities",
  "panel_annotations": "Annotaties & Links",
  "panel_no_annotations": "Nog geen annotaties.",
  "panel_edit": "Bewerk deze persoon",
  "panel_save": "Opslaan",
  "panel_cancel": "Annuleren",
  "panel_sex_male": "Man",
  "panel_sex_female": "Vrouw",
  "panel_sex_unknown": "Onbekend",
  "timeline_heading": "Tijdlijn \u2014 kortste route ({n} personen)",
  "timeline_estimated": "geschat",
  "timeline_no_graph": "Graph data niet beschikbaar.",
  "timeline_no_connection": "Geen verbinding gevonden tussen deze twee personen.",
  "timeline_no_years": "Geen jaartallen beschikbaar voor tijdlijn.",
  "timeline_button": "Tijdlijn",
  "back_to_single": "Terug naar {name}",
  "admin_brand": "Stamboom Beheer",
  "admin_persons": "Personen",
  "admin_relationships": "Relaties",
  "admin_import": "Importeer data",
  "admin_export": "Exporteer data",
  "admin_viz_link": "\u2190 Visualisatie",
  "admin_change_password": "Wachtwoord wijzigen",
  "admin_logout": "Uitloggen",
  "admin_login_title": "Admin login",
  "admin_login_password": "Wachtwoord",
  "admin_login_button": "Inloggen",
  "admin_add_person": "+ Persoon toevoegen",
  "admin_search_placeholder": "Zoek op naam\u2026",
  "admin_import_title": "Importeer data",
  "admin_import_desc": "Upload een .ged (GEDCOM), .gramps (GRAMPS XML), of .xml (generiek XML) bestand.",
  "admin_import_warning": "Dit vervangt alle huidige data.",
  "admin_import_button": "Importeren",
  "admin_import_file_label": "Databestand",
  "admin_import_mapping_label": "XML-veldmapping (JSON)"
}
```

**public/js/i18n/en.json** — English translations (fallback). Mirror all keys from nl.json with English values:
```json
{
  "lang_toggle_label": "NL",
  "cluster_auto": "Clusters: auto",
  "cluster_locked": "Clusters: locked",
  "cluster_level_label": "Cluster level",
  "cluster_centuries": "Centuries",
  "cluster_surnames": "Surnames",
  "cluster_persons": "Persons",
  "cluster_expand_tooltip": "Click to expand",
  "rel_prefix": "Relationship:",
  "rel_ancestor": "Ancestor ({n} generations)",
  "rel_descendant": "Descendant ({n} generations)",
  "rel_spouse": "Spouse",
  "rel_inlaw": "In-law relative (via {n} steps)",
  "rel_complex": "Related via {n} steps",
  "rel_no_path": "No connection found",
  "rel_error": "Could not determine relationship",
  "comparison_heading": "Comparison",
  "rel_section_heading": "Relationship",
  "toolbar_role": "By Role",
  "toolbar_surname": "By Surname",
  "toolbar_sex": "By Sex",
  "toolbar_overlap": "Overlap",
  "panel_vitals": "Vitals",
  "panel_roles": "Roles",
  "panel_connections": "Connections",
  "panel_notes": "Notes",
  "panel_annotations": "Annotations & Links",
  "panel_no_annotations": "No annotations yet.",
  "panel_edit": "Edit this person",
  "panel_save": "Save",
  "panel_cancel": "Cancel",
  "panel_sex_male": "Male",
  "panel_sex_female": "Female",
  "panel_sex_unknown": "Unknown",
  "timeline_heading": "Timeline \u2014 shortest route ({n} persons)",
  "timeline_estimated": "estimated",
  "timeline_no_graph": "Graph data not available.",
  "timeline_no_connection": "No connection found between these two persons.",
  "timeline_no_years": "No year data available for timeline.",
  "timeline_button": "Timeline",
  "back_to_single": "Back to {name}",
  "admin_brand": "Family Tree Admin",
  "admin_persons": "Persons",
  "admin_relationships": "Relationships",
  "admin_import": "Import data",
  "admin_export": "Export data",
  "admin_viz_link": "\u2190 Visualisation",
  "admin_change_password": "Change password",
  "admin_logout": "Log out",
  "admin_login_title": "Admin login",
  "admin_login_password": "Password",
  "admin_login_button": "Log in",
  "admin_add_person": "+ Add person",
  "admin_search_placeholder": "Search by name\u2026",
  "admin_import_title": "Import data",
  "admin_import_desc": "Upload a .ged (GEDCOM), .gramps (GRAMPS XML), or .xml (generic XML) file.",
  "admin_import_warning": "This replaces all current data.",
  "admin_import_button": "Import",
  "admin_import_file_label": "Data file",
  "admin_import_mapping_label": "XML field mapping (JSON)"
}
```

IMPORTANT: The `fetch()` URL must be `'js/i18n/' + l + '.json'` (no leading slash) so it resolves relative to the `<base href="/genealogy-viz/">` tag present in both `app.html` and `admin/index.html`. Do NOT use an absolute path.

IMPORTANT: `applyToDOM()` must be called both in `init()` and after each `setLang()` call. It is idempotent per Pitfall 4 in RESEARCH.md.
  </action>
  <verify>
    <automated>test -f public/js/i18n.js && test -f public/js/i18n/nl.json && test -f public/js/i18n/en.json && node -e "const nl=require('./public/js/i18n/nl.json'); const en=require('./public/js/i18n/en.json'); const nlKeys=Object.keys(nl); const enKeys=Object.keys(en); if(nlKeys.length<30) throw 'Too few nl keys: '+nlKeys.length; if(enKeys.length<30) throw 'Too few en keys: '+enKeys.length; const missing=nlKeys.filter(k=>!enKeys.includes(k)); if(missing.length) throw 'en.json missing keys: '+missing.join(', '); console.log('OK: '+nlKeys.length+' keys in both files')"</automated>
  </verify>
  <acceptance_criteria>
    - public/js/i18n.js contains `window.i18n = (function`
    - public/js/i18n.js contains `localStorage.getItem('lang')`
    - public/js/i18n.js contains `localStorage.setItem('lang'`
    - public/js/i18n.js contains `function t(key)`
    - public/js/i18n.js contains `function toggleLang()`
    - public/js/i18n.js contains `function applyToDOM()`
    - public/js/i18n.js contains `querySelectorAll('[data-i18n]')`
    - public/js/i18n.js contains `querySelectorAll('[data-i18n-title]')`
    - public/js/i18n/nl.json contains `"lang_toggle_label": "EN"`
    - public/js/i18n/en.json contains `"lang_toggle_label": "NL"`
    - public/js/i18n/nl.json contains `"rel_ancestor"`
    - public/js/i18n/nl.json contains `"admin_brand"`
    - nl.json and en.json have identical key sets (verified by automated command)
  </acceptance_criteria>
  <done>i18n module file exists with t(), init(), setLang(), toggleLang(), applyToDOM(), getLang(). Both JSON files have 30+ matching keys covering all UI-SPEC copywriting entries.</done>
</task>

<task type="auto">
  <name>Task 2: Add language toggle button to app.html toolbar and wire i18n init</name>
  <files>public/app.html</files>
  <read_first>
    - public/app.html (current toolbar HTML and script load order)
    - public/js/i18n.js (the module just created in Task 1)
    - public/js/viz.js (understand DOMContentLoaded -> initViz flow)
    - .planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-UI-SPEC.md (toolbar language toggle interaction contract)
  </read_first>
  <action>
Modify `public/app.html` to:

1. Add `<script src="js/i18n.js"></script>` as the FIRST script tag (before colour-modes.js) so that `window.i18n` is available to all other modules. The script order must be:
```html
<script src="js/i18n.js"></script>
<script src="js/colour-modes.js"></script>
<script src="js/overlap-logic.js"></script>
<script src="js/side-panel.js"></script>
<script src="js/viz.js"></script>
```

2. Add language toggle button inside `#toolbar` div, AFTER the overlap button (per D-06 — "next to the existing colour mode and overlap buttons"). Per UI-SPEC: label shows what you switch TO (EN when Dutch, NL when English). The button uses `data-i18n="lang_toggle_label"` so it self-updates:
```html
<button class="toolbar-btn" id="btn-lang" data-i18n="lang_toggle_label" onclick="i18n.toggleLang()">EN</button>
```

3. Add `data-i18n` attributes to the four existing colour mode buttons so their labels translate:
```html
<button class="toolbar-btn active" id="btn-role" data-i18n="toolbar_role" onclick="setColorMode('role')">By Role</button>
<button class="toolbar-btn" id="btn-surname" data-i18n="toolbar_surname" onclick="setColorMode('surname')">By Surname</button>
<button class="toolbar-btn" id="btn-sex" data-i18n="toolbar_sex" onclick="setColorMode('sex')">By Sex</button>
<button class="toolbar-btn" id="btn-overlap" data-i18n="toolbar_overlap" onclick="setColorMode('overlap')" data-i18n-title="toolbar_overlap_title">Overlap</button>
```

4. Modify the `DOMContentLoaded` init in viz.js integration: add a small inline script at the bottom of the body (AFTER viz.js) that calls `i18n.init()` before `initViz()`. Alternatively, modify `initViz()` in `viz.js` to await `i18n.init()` first. The simplest approach: replace the `document.addEventListener('DOMContentLoaded', initViz)` line in `viz.js` with a version that awaits i18n first. BUT since this plan only owns `app.html`, add an inline script:
```html
<script>
  document.addEventListener('DOMContentLoaded', async function() {
    await i18n.init();
    await initViz();
  });
</script>
```
And correspondingly, the `document.addEventListener('DOMContentLoaded', initViz)` line at the bottom of `viz.js` must be REMOVED. To avoid modifying viz.js in this plan (it belongs to Plan 04), instead use this approach: Keep the inline script AND leave viz.js as-is. The duplicate call is safe because `initViz()` is idempotent (it fetches data and creates the graph; a second call would re-fetch but the first one wins in practice). ACTUALLY this will cause a double init. Better approach: Remove the DOMContentLoaded listener from viz.js. This means modifying viz.js minimally.

REVISED: Modify BOTH `public/app.html` and `public/js/viz.js`:
- In `public/js/viz.js`: Remove the last line `document.addEventListener('DOMContentLoaded', initViz);`
- In `public/app.html`: Add inline init script after all script tags:
```html
<script>
  document.addEventListener('DOMContentLoaded', async function() {
    await i18n.init();
    await initViz();
  });
</script>
```
  </action>
  <verify>
    <automated>grep -n 'js/i18n.js' public/app.html && grep -n 'btn-lang' public/app.html && grep -n 'data-i18n=' public/app.html && grep -n 'i18n.init' public/app.html && ! grep -q 'DOMContentLoaded.*initViz' public/js/viz.js && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - public/app.html contains `src="js/i18n.js"` BEFORE `src="js/colour-modes.js"` in document order
    - public/app.html contains `id="btn-lang"` with `data-i18n="lang_toggle_label"`
    - public/app.html contains `onclick="i18n.toggleLang()"`
    - public/app.html contains `data-i18n="toolbar_role"` on the role button
    - public/app.html contains `data-i18n="toolbar_surname"` on the surname button
    - public/app.html contains `data-i18n="toolbar_sex"` on the sex button
    - public/app.html contains `data-i18n="toolbar_overlap"` on the overlap button
    - public/app.html contains `await i18n.init()` before `await initViz()`
    - public/js/viz.js does NOT contain `DOMContentLoaded` (removed)
  </acceptance_criteria>
  <done>Language toggle button visible in toolbar. i18n initialises before viz. All toolbar buttons have data-i18n attributes for translation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetch → JSON files | i18n JSON loaded from same-origin static files via fetch() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T (Tampering) | i18n JSON files | accept | JSON files are static assets served by Express; tampering requires server compromise which is out of Phase 3 scope. No user-supplied content enters the JSON files. |
| T-03-02 | I (Information Disclosure) | localStorage lang key | accept | Language preference is non-sensitive. No PII stored. |
</threat_model>

<verification>
1. Open app.html in browser — language toggle button "EN" visible in toolbar
2. Click "EN" — all toolbar labels switch to Dutch, button text becomes "NL"
3. Reload page — language preference persists (still Dutch)
4. Verify console has no errors on page load
</verification>

<success_criteria>
- `window.i18n.t('lang_toggle_label')` returns "EN" when lang=nl, "NL" when lang=en
- Language toggle button appears in toolbar after overlap button
- localStorage key "lang" set on toggle
- Both JSON files have identical key sets with 30+ entries
- No console errors on page load
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-01-SUMMARY.md`
</output>
