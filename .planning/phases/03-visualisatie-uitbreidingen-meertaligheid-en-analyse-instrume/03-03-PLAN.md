---
phase: 03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - public/js/side-panel.js
  - public/admin/index.html
autonomous: true
requirements: [I18N-02, REL-02]

must_haves:
  truths:
    - "Side panel labels switch between Dutch and English when language is toggled"
    - "Admin panel labels switch between Dutch and English when language is toggled"
    - "Relationship label appears below timeline in two-person comparison mode"
    - "Relationship label shows correct Dutch name (e.g. Voorouder, Nakomeling) by default"
    - "Relationship label shows English name when language is switched to English"
  artifacts:
    - path: "public/js/side-panel.js"
      provides: "i18n-aware side panel rendering with relationship label"
      contains: "i18n.t("
    - path: "public/admin/index.html"
      provides: "Admin panel with i18n script and data-i18n attributes"
      contains: "data-i18n"
  key_links:
    - from: "public/js/side-panel.js"
      to: "public/js/i18n.js"
      via: "i18n.t() calls in renderPanel and renderTwoPersonMode"
      pattern: "i18n\\.t\\("
    - from: "public/js/side-panel.js"
      to: "public/js/rel-path.js"
      via: "RelPath.classifyRelationship() in renderTwoPersonMode"
      pattern: "RelPath\\.classifyRelationship"
    - from: "public/admin/index.html"
      to: "public/js/i18n.js"
      via: "script tag loading i18n.js"
      pattern: "src=\"js/i18n.js\""
---

<objective>
Integrate i18n into all UI surfaces and wire the relationship label into the side panel's two-person comparison view.

Purpose: Complete D-08 (all UI labels translated), D-13 (relationship label display in side panel). This wires the i18n and rel-path modules from Plans 01 and 02 into the actual UI.
Output: Fully translated side panel and admin panel; relationship label visible in two-person comparison.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-CONTEXT.md
@.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-RESEARCH.md
@.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-UI-SPEC.md
@.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-01-SUMMARY.md
@.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: i18n module -->
From public/js/i18n.js:
```javascript
window.i18n = {
  init(),           // async — loads lang from localStorage, fetches JSON, applies to DOM
  t(key),           // returns translated string for current lang
  setLang(l),       // async — switches language
  toggleLang(),     // toggles nl <-> en
  applyToDOM(),     // updates all [data-i18n] and [data-i18n-title] elements
  getLang(),        // returns current lang string ('nl' or 'en')
};
```

<!-- From Plan 02: RelPath module -->
From public/js/rel-path.js:
```javascript
window.RelPath = {
  classifyRelationship(path, graphData),  // returns { type, hops } or null
  formatRelLabel(result, t),              // returns localized string using t() function
};
```

<!-- From existing side-panel.js: key functions -->
From public/js/side-panel.js:
```javascript
function renderPanel(data) { ... }           // lines 49-118
function renderTwoPersonMode() { ... }       // lines 120-199
function escHtml(str) { ... }                // line 281
// currentData, secondPersonData, currentPersonId, secondPersonId — module-level vars
```

<!-- From existing admin/index.html: hardcoded English labels -->
Key elements to add data-i18n to:
- <span class="brand">Genealogy Admin</span>
- <a href="#persons">Persons</a>
- <a href="#import">Import data</a>
- <h1 class="page-title">Persons</h1>
- <h1 class="page-title">Import data</h1>
- <h1>Admin login</h1>
- Various form labels
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded strings in side-panel.js with i18n.t() calls and add relationship label</name>
  <files>public/js/side-panel.js</files>
  <read_first>
    - public/js/side-panel.js (full file — current renderPanel and renderTwoPersonMode functions)
    - public/js/i18n/nl.json (translation keys to use)
    - public/js/rel-path.js (RelPath.classifyRelationship and formatRelLabel signatures)
    - public/js/overlap-logic.js (shortestPath signature — already used in renderTwoPersonMode)
    - .planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-UI-SPEC.md (side panel relationship label interaction contract)
  </read_first>
  <action>
Modify `public/js/side-panel.js` to replace ALL hardcoded English strings with `i18n.t(key)` calls. Also add the relationship label section to `renderTwoPersonMode()`.

**In `renderPanel(data)`** (around lines 49-118), replace these hardcoded strings:
- `'Vitals'` → `i18n.t('panel_vitals')`
- `'Male'` / `'Female'` / `'Unknown'` → `i18n.t('panel_sex_male')` / `i18n.t('panel_sex_female')` / `i18n.t('panel_sex_unknown')`
- `'Roles'` → `i18n.t('panel_roles')`
- `'Connections'` → `i18n.t('panel_connections')`
- `'Notes'` → `i18n.t('panel_notes')`
- `'Annotations & Links'` → `i18n.t('panel_annotations')`
- `'No annotations yet.'` → `i18n.t('panel_no_annotations')`
- `'Edit this person'` → `i18n.t('panel_edit')`
- `'Save'` → `i18n.t('panel_save')`
- `'Cancel'` → `i18n.t('panel_cancel')`
- `'Tijdlijn'` button label → `i18n.t('timeline_button')`

Example transformation in renderPanel:
```javascript
// Before:
html += `<div class="panel-section">
  <div class="panel-label">Vitals</div>
  <div>${data.sex === 'M' ? 'Male' : data.sex === 'F' ? 'Female' : 'Unknown'}</div>`;

// After:
html += '<div class="panel-section">' +
  '<div class="panel-label">' + escHtml(i18n.t('panel_vitals')) + '</div>' +
  '<div>' + (data.sex === 'M' ? escHtml(i18n.t('panel_sex_male')) : data.sex === 'F' ? escHtml(i18n.t('panel_sex_female')) : escHtml(i18n.t('panel_sex_unknown'))) + '</div>';
```

**In `renderTwoPersonMode()`** (around lines 120-199), replace:
- `'Graph data niet beschikbaar.'` → `i18n.t('timeline_no_graph')`
- `'Geen verbinding gevonden tussen deze twee personen.'` → `i18n.t('timeline_no_connection')`
- `'Geen jaartallen beschikbaar voor tijdlijn.'` → `i18n.t('timeline_no_years')`
- The timeline heading `'Tijdlijn — kortste route (N personen)'` → `i18n.t('timeline_heading').replace('{n}', path.length)`
- `'geschat'` → `i18n.t('timeline_estimated')`
- The back button `'Terug naar ...'` → `i18n.t('back_to_single').replace('{name}', escHtml(...))`

**Add relationship label section** in `renderTwoPersonMode()`, AFTER the closing `</div>` of the timeline panel-section (after the `pathNodes.forEach` loop and its closing `html += '</div>'`), and BEFORE the back button:

```javascript
// Relationship label section (per D-10, D-11, D-13)
html += '<div class="panel-section">';
html += '<div class="panel-label">' + escHtml(i18n.t('rel_section_heading')) + '</div>';
if (window.RelPath) {
  var relResult = RelPath.classifyRelationship(path, window.graphData);
  var relLabel = RelPath.formatRelLabel(relResult, i18n.t);
  html += '<p style="font-size:14px;color:#c9d1d9">' +
    '<span style="color:#6e7681">' + escHtml(i18n.t('rel_prefix')) + ' </span>' +
    escHtml(relLabel) + '</p>';
} else {
  html += '<p style="font-size:14px;color:#6e7681">' + escHtml(i18n.t('rel_error')) + '</p>';
}
html += '</div>';
```

Per UI-SPEC: The relationship section uses `.panel-section` with `border-bottom: 1px solid #21262d`, `.panel-label` uppercase header at 11px, and body text at 14px. The prefix "Relatie:" is in `#6e7681` muted colour and the label text is in `#c9d1d9` standard body colour.

IMPORTANT: Continue using `escHtml()` for ALL dynamic text inserted into innerHTML — this includes the relationship label string (per RESEARCH.md security section).

Also add the `<script src="js/rel-path.js"></script>` to `public/app.html` in the script load order. Wait — app.html is not in this plan's files_modified. The script tag for rel-path.js should be added. Let me include app.html as a minor touch.

REVISED: Also modify `public/app.html` to add `<script src="js/rel-path.js"></script>` after `overlap-logic.js` and before `side-panel.js`:
```html
<script src="js/i18n.js"></script>
<script src="js/colour-modes.js"></script>
<script src="js/overlap-logic.js"></script>
<script src="js/rel-path.js"></script>
<script src="js/side-panel.js"></script>
<script src="js/viz.js"></script>
```

Additionally, the i18n language switch must trigger a re-render of the currently open panel. Add to the bottom of side-panel.js:
```javascript
// Re-render panel on language change
window.addEventListener('langchange', function() {
  if (secondPersonId && secondPersonData) {
    renderTwoPersonMode();
  } else if (currentData) {
    renderPanel(currentData);
  }
});
```

And in `public/js/i18n.js`, the `setLang()` function should dispatch a custom event after applying to DOM. BUT i18n.js is owned by Plan 01. Instead, make the side panel listen for `storage` events or use a simpler approach: modify `i18n.js` to accept a callback list. SIMPLEST: dispatch a CustomEvent from setLang. Since i18n.js was created in Plan 01, we can add one line to its setLang function. Add to the end of `applyToDOM()` in i18n.js:
```javascript
window.dispatchEvent(new CustomEvent('langchange'));
```
This is a minor addition to i18n.js — add it during this task.
  </action>
  <verify>
    <automated>grep -c "i18n.t(" public/js/side-panel.js && grep "RelPath.classifyRelationship" public/js/side-panel.js && grep "rel-path.js" public/app.html && grep "langchange" public/js/side-panel.js && grep "langchange" public/js/i18n.js && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - public/js/side-panel.js contains at least 15 occurrences of `i18n.t(`
    - public/js/side-panel.js contains `RelPath.classifyRelationship(path, window.graphData)`
    - public/js/side-panel.js contains `RelPath.formatRelLabel(relResult, i18n.t)`
    - public/js/side-panel.js contains `i18n.t('panel_vitals')`
    - public/js/side-panel.js contains `i18n.t('panel_connections')`
    - public/js/side-panel.js contains `i18n.t('rel_section_heading')`
    - public/js/side-panel.js contains `i18n.t('rel_prefix')`
    - public/js/side-panel.js does NOT contain the hardcoded string `'Vitals'` (replaced with i18n key)
    - public/js/side-panel.js does NOT contain the hardcoded string `'Connections'` or `'Roles'` as panel labels
    - public/js/side-panel.js contains `addEventListener('langchange'`
    - public/app.html contains `src="js/rel-path.js"` BEFORE `src="js/side-panel.js"`
    - public/js/i18n.js contains `dispatchEvent` with `langchange`
  </acceptance_criteria>
  <done>All side panel strings use i18n.t(). Relationship label section appears in two-person mode with prefix and named label. Language change triggers panel re-render.</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n to admin panel</name>
  <files>public/admin/index.html</files>
  <read_first>
    - public/admin/index.html (full file — all hardcoded English labels)
    - public/js/i18n/nl.json (admin-related translation keys)
    - public/js/i18n.js (data-i18n attribute pattern, applyToDOM)
    - .planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-RESEARCH.md (Pitfall 5 — admin panel separate page context)
  </read_first>
  <action>
Modify `public/admin/index.html` to:

1. Add `<script src="js/i18n.js"></script>` BEFORE the existing `<script src="js/admin.js"></script>` at the bottom of body. The path `js/i18n.js` resolves correctly because admin/index.html has `<base href="/genealogy-viz/">` (per Pitfall 5 in RESEARCH.md).

2. Add `data-i18n` attributes to all static text elements:

Nav bar:
```html
<span class="brand" data-i18n="admin_brand">Genealogy Admin</span>
<a href="#persons" class="active" data-i18n="admin_persons" onclick="showTab(event,'persons',this)">Persons</a>
<a href="#relationships" data-i18n="admin_relationships" onclick="showTab(event,'relationships',this)">Relationships</a>
<a href="#import" data-i18n="admin_import" onclick="showTab(event,'import',this)">Import data</a>
<a href="#" data-i18n="admin_export" onclick="event.preventDefault();doExport()">Export data</a>
<a href="/genealogy-viz/" data-i18n="admin_viz_link" style="margin-left:auto">← Visualisation</a>
<a href="#" data-i18n="admin_change_password" onclick="event.preventDefault();showChangePasswordModal()">Change password</a>
<a href="#" data-i18n="admin_logout" onclick="event.preventDefault();doLogout()" style="color:#f85149">Log out</a>
```

Page titles:
```html
<h1 class="page-title" data-i18n="admin_persons" style="margin:0">Persons</h1>
```
(Note: the Persons title in tab-persons div)

Import tab:
```html
<h1 class="page-title" data-i18n="admin_import_title">Import data</h1>
```

Login overlay:
```html
<h1 style="..." data-i18n="admin_login_title">Admin login</h1>
```

Search box:
```html
<input class="search-box" data-i18n-title="admin_search_placeholder" placeholder="Search by name…" oninput="filterPersons(this.value)">
```
Note: For placeholder text, use a special approach. `data-i18n` sets `textContent` which does not work for `placeholder`. Either extend `applyToDOM()` to handle `data-i18n-placeholder` OR just use `data-i18n-placeholder` and add this to `applyToDOM()` in i18n.js. Add to i18n.js `applyToDOM()`:
```javascript
document.querySelectorAll('[data-i18n-placeholder]').forEach(function(el) {
  el.placeholder = t(el.dataset.i18nPlaceholder);
});
```

So the search box becomes:
```html
<input class="search-box" data-i18n-placeholder="admin_search_placeholder" placeholder="Search by name…" oninput="filterPersons(this.value)">
```

Add person button:
```html
<button class="btn btn-primary" data-i18n="admin_add_person" onclick="showPersonForm(null)">+ Add person</button>
```

Import button and description paragraphs: These contain mixed HTML content (bold, spans), so `data-i18n` (which sets textContent) would strip the HTML. For the description paragraph, use a simpler approach: set the `data-i18n` only on elements that contain pure text. For the paragraph with `<strong>` and `<span>`, leave it as-is (it is admin-only, low priority).

3. Add language toggle in the admin nav (after the brand, before first tab link):
```html
<button class="toolbar-btn" id="admin-btn-lang" data-i18n="lang_toggle_label" onclick="i18n.toggleLang()" style="padding:4px 8px;font-size:11px;margin-right:8px">EN</button>
```

4. Add inline init script after admin.js:
```html
<script>
  document.addEventListener('DOMContentLoaded', function() {
    if (window.i18n) i18n.init();
  });
</script>
```

5. Update `public/js/i18n.js` to also handle `data-i18n-placeholder` in `applyToDOM()`:
```javascript
document.querySelectorAll('[data-i18n-placeholder]').forEach(function(el) {
  el.placeholder = t(el.dataset.i18nPlaceholder);
});
```
  </action>
  <verify>
    <automated>grep -c "data-i18n" public/admin/index.html && grep "js/i18n.js" public/admin/index.html && grep "admin-btn-lang" public/admin/index.html && grep "data-i18n-placeholder" public/js/i18n.js && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - public/admin/index.html contains at least 10 `data-i18n` attributes
    - public/admin/index.html contains `src="js/i18n.js"` BEFORE `src="js/admin.js"`
    - public/admin/index.html contains `id="admin-btn-lang"` with `data-i18n="lang_toggle_label"`
    - public/admin/index.html contains `i18n.init()` in a DOMContentLoaded handler
    - public/admin/index.html contains `data-i18n="admin_brand"` on the brand span
    - public/admin/index.html contains `data-i18n="admin_persons"` on at least one element
    - public/admin/index.html contains `data-i18n="admin_import"` on the import nav link
    - public/admin/index.html contains `data-i18n-placeholder="admin_search_placeholder"` on the search input
    - public/js/i18n.js contains `data-i18n-placeholder` in applyToDOM
  </acceptance_criteria>
  <done>Admin panel has i18n script loaded, language toggle in nav, data-i18n attributes on all pure-text labels, and placeholder translation support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n.t() → innerHTML | Translated strings injected into panel via innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | S (Spoofing) | i18n strings in innerHTML | mitigate | All i18n.t() output is wrapped in escHtml() before insertion into innerHTML. Verified: escHtml() is already used throughout side-panel.js and must continue to wrap every dynamic string. |
| T-03-05 | T (Tampering) | RelPath label in innerHTML | mitigate | formatRelLabel output is passed through escHtml() before innerHTML insertion. The label may contain user-visible numbers but no user-supplied content. |
</threat_model>

<verification>
1. Open app.html, click a person, verify side panel labels are in Dutch
2. Toggle language to EN, verify all side panel labels switch to English
3. Open two-person comparison, verify relationship label appears below timeline
4. Open admin/index.html, verify Dutch labels, toggle to English
5. No console errors on either page
</verification>

<success_criteria>
- All hardcoded English strings in side-panel.js replaced with i18n.t() calls
- Relationship label visible in two-person comparison with correct Dutch/English text
- Admin panel loads i18n.js and translates all pure-text labels
- Language switch triggers panel re-render via langchange event
- escHtml() wraps all dynamic text in innerHTML
</success_criteria>

<output>
After completion, create `.planning/phases/03-visualisatie-uitbreidingen-meertaligheid-en-analyse-instrume/03-03-SUMMARY.md`
</output>
